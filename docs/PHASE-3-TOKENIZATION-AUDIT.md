# Phase 3 - Page Tokenization Compliance Audit Report

**Date:** 2026-02-01
**Audited Directory:** Pages/
**Token Reference:** wwwroot/css/tokens.css

---

## Executive Summary

This audit scanned all `.cshtml` files for hardcoded design values that should use CSS design tokens from `tokens.css`.

### Statistics

- **Total Files Scanned**: 100+ `.cshtml` files
- **Files with Violations**: 46+ files
- **Total Violations**: 500+ individual instances
- **Categories**: 9 violation types

---

## Critical Violations (Must Fix)

### 1. Hardcoded `color: white` (136+ instances in 30+ files)

Files affected:
- Pages/Shared/_Layout.cshtml (lines 257, 577)
- Pages/Home/Index.cshtml (line 102)
- Pages/Director/ViewAsMode.cshtml (lines 24, 31)
- Pages/Director/CompanyFilter.cshtml (line 24)
- Pages/Calendar/Week.cshtml (line 588)
- Pages/Calendar/Table.cshtml (lines 184, 399, 434, 489, 607, 643, 685, 699, 769, 829, 1211)
- Pages/Admin/Users.cshtml (lines 201, 213, 223, 300, 305)
- Pages/My/ApiKeys.cshtml (20+ instances)
- Pages/My/NotificationCenter.cshtml (lines 24, 33, 40, 92, 102)
- Pages/My/Index.cshtml (lines 69, 147)
- Pages/Owner/LanguageManagement.cshtml (lines 91, 97, 271, 277, 299, 374, 517)
- Pages/Auth/Logout.cshtml (line 28)
- Pages/Public/Feedback.cshtml (multiple)
- Pages/Friends/Index.cshtml (lines 27-34)
- Pages/Admin/Config.cshtml (line 569)
- And 15+ more files...

**Fix:** Replace with `var(--primary-contrast)` or appropriate semantic token.

### 2. Hardcoded `color: red` (2 instances)

- Pages/Public/Feedback.cshtml (lines 249, 258)

**Fix:** Replace with `var(--danger)`.

---

## High Priority Violations

### 3. Hardcoded RGBA Colors (100+ instances)

Modal overlays and opacity values using `rgba(0,0,0,0.5)` etc.

**Fix:** Create `--overlay-bg` token, use `rgba(var(--primary-rgb), opacity)` pattern.

### 4. Hardcoded Spacing (200+ instances in 35+ files)

Files using `padding: 1rem`, `margin: 2rem`, etc. instead of `var(--space-*)`.

### 5. Hardcoded Font Sizes (40+ instances in 15+ files)

Files using `font-size: 0.875rem` instead of `var(--text-small)`.

### 6. Hardcoded Border Radius

Files using `border-radius: 8px` instead of `var(--radius-md)`.

---

## Token Reference Quick Guide

| Hardcoded | Token |
|-----------|-------|
| `white` | `var(--primary-contrast)` or `var(--surface)` |
| `red` | `var(--danger)` |
| `padding: 0.5rem` | `padding: var(--space-2)` |
| `padding: 1rem` | `padding: var(--space-4)` |
| `padding: 2rem` | `padding: var(--space-8)` |
| `margin: 1.5rem` | `margin: var(--space-6)` |
| `font-size: 0.875rem` | `font-size: var(--text-small)` |
| `font-size: 0.75rem` | `font-size: var(--text-tiny)` |
| `font-weight: 600` | `font-weight: var(--font-semibold)` |
| `border-radius: 4px` | `border-radius: var(--radius-sm)` |
| `border-radius: 8px` | `border-radius: var(--radius-md)` |
| `border-radius: 50%` | `border-radius: var(--radius-full)` |
| `rgba(0,0,0,0.5)` | `var(--overlay-bg)` |

---

## Implementation Priority

1. **Phase 3a**: Replace all `color: white` and `color: red` - 138 instances
2. **Phase 3b**: Add `--overlay-bg` token and fix modal overlays - 10 instances
3. **Phase 3c**: Replace hardcoded spacing in high-traffic pages - 200+ instances
4. **Phase 3d**: Replace hardcoded typography - 70+ instances

---

*Generated by Claude Code - Phase 3 Re-audit*
