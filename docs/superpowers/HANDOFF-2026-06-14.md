# Session Handoff — 2026-06-13/14

## TL;DR
The **entire 4-phase backlog (all 17 items) is implemented, verified, and committed on `dev`.** Full test suite **1587/1587** green. App runs on `localhost:5000`. **Nothing is merged to `release`** yet. There are **no not-yet-started backlog tasks** — what remains is a list of small deferred follow-ups + autonomous design decisions to sanity-check (below).

The three you flagged as most important are all done: **Quick Entry (all 3 issues) fixed, mail-override disable toggle added, Hebrew fonts changed.**

---

## Backlog status — every item

### Phase 1 — Zero-Bugs Assignment ✅
| # | Item | Status | Commit |
|---|------|--------|--------|
| 1.1 | Quick Entry lockup | ✅ fixed + verified | `18e51ca` |
| 1.2 | Quick Entry notes | ✅ fixed (same root cause as 1.1) + verified | `18e51ca` |
| 1.3 | Enter to confirm | ✅ fixed + verified | `18e51ca` |
| 1.4 | Drag-reorder Shifts (both modes) | ✅ built + verified | `af6b2ec`…`f0fd645`* |
| 1.5 | Drag-reorder global + categories (incl. Overview) | ✅ built + verified | reorder feature (9 commits) |
*reorder feature commits: `af6b2ec, 9a87ad4, 08842c9, 5387416, f76b9f3, bd38664, 627d8ff, d4b7997, 5957694, 95db66e` + spec/plan `a8ddc98, acee37b`.

### Phase 2 — Navigation & Context ✅
| # | Item | Status | Commit |
|---|------|--------|--------|
| 2.1 | Context switcher on Overview | ✅ fixed + verified | `faad862` (in Overview.cshtml.cs) |
| 2.2 | Date picker click | ✅ fixed + verified | `4719dfc` |
| 2.3 | Force-delete user + root cause | ✅ fixed + verified (both normal + force paths) | `faad862`, `bdc6531` |

### Phase 3 — UI Tweaks ✅
| # | Item | Status | Commit |
|---|------|--------|--------|
| 3.1 | Hide תפוצות button | ✅ done | `0cb6d93` |
| 3.2 | "Next 7 days" view | ✅ done on **all 4 calendars** | `0cb6d93`, `aefa4c1` |
| 3.3 | Hebrew fonts | ✅ done | `7bc748a` |
| 3.4 | Auth-hero emojis | ✅ done | `95d13f0` |

### Phase 4 — New Features ✅
| # | Item | Status | Commit |
|---|------|--------|--------|
| 4.1 | Admin vacation requests | ✅ built + verified | `453106b` |
| 4.2 | "טכנו" (Techno) job type | ✅ built + verified | `8473c37` |
| 4.3 | Owner email-override dashboard + disable toggle | ✅ built + verified | `590d29b` |
| 4.4 | Test Summon mail | ✅ built (source-verified; see deferred #2) | `5f3f524` |
| 4.5 | "Who is on shift" home dashboard | ✅ built + verified; gate = CO + Area Admin | `20886d0`, `f0fd645`, gate fix latest |

---

## DEFERRED ITEMS / open follow-ups (next session)

1. **4.1 — approver dropdown empty on `/Requests/Index`.** Admins CAN request there, but the "approver" dropdown shows only the default because `Pages/Requests/Index.cshtml.cs` (`IndexModel`) doesn't load the approver pool (that LINQ lives only in `Pages/My/Requests.cshtml.cs`). Auto-routing assigns an approver, so it works — but to let admins *pick* a specific approver from that page, port the approver-loading query into `IndexModel` and pass it to the `_TimeOffRequestForm` partial. **Risk: medium.**

2. **4.4 — Test Summon not browser-tested.** Email isn't configured in dev (`EmailEnabled && HasExistingKey` is false), so neither the Test-Email nor the new Test-Summon button renders. Verified at source (handler `OnPostSendTestSummonAsync`, form, resx all present, build green). **To confirm live: configure an email API key in `/Owner/EmailConfig`, then both buttons appear; click Test Summon and check the calendar invite.**

3. **2.1 — multi-company *member* mode not E2E-tested.** Verified via the **owner** company switch (same `_tenantResolver.GetCurrentTenantId()` path the fix uses). There is **no 2-company member account in the seed** to exercise the `member_selected_company` cookie path directly. To fully validate: create a user with two `CompanyMembership` rows, log in, switch via the sidebar switcher, confirm Overview follows.

4. **1.2 — "every user" notes nuance.** Employees and Trainees **can already** write notes (verified, HTTP 200). A *truly grant-less* user (no `WriteOverviewNotes`, no edit grant) still doesn't see the Quick Entry toggle. I did **not** extend note-writing to that tier — decide if you want every role, including grant-less, to write notes (would be a grant-policy change to `WriteOverviewNotes` assignment).

5. **2.3 — force-delete confirm-modal not click-tested in browser.** The server force path is proven (deleted a user with 6 authored on-duties + 17 chores, reassigned to admin, 0 orphans). The "Force delete anyway" button renders on the blocked-delete response (confirmed in the response HTML), but I didn't click through the `data-confirm-modal` dialog in a real browser.

---

## Autonomous design decisions to sanity-check
You delegated these overnight ("decide what I'd choose"):
- **4.3 disable semantics:** chose a **reversible soft toggle** — added `EmailConfig.OverrideEnabled` (default true); the resolver returns a company override only when `OverrideEnabled`, else falls back to global. The existing destructive "Revert to Global" (delete) is untouched. *Alternative you might prefer: hard delete only.*
- **4.1:** surfaced the existing request form on `/Requests/Index` via a shared partial posting to `/My/Requests` (low-risk, no logic refactor); hid a requester's own request from their own approval list.
- **4.5 gate:** `EditArea` (Area Admin) **OR** `EditMolecule` (CO/מפק"מ = `MoleculeAdmin` role) **OR** `AdminAccess` (Owner). Verified: CO + Area Admin see it, Employee doesn't.
- **4.5 "currently on":** a user counts as on-shift if `now` is within the shift's `[Start,End]` window **today** (overnight-aware). Empty cubes still show for monitored shifts with no one on.
- **4.2 Techno:** added via seed **and** an idempotent `Program.cs` catch-up so your **existing** `app.db` got it (the org seed is one-time).

---

## Environment / data notes
- **Branch `dev`** — 30+ commits this session, working tree clean. Not merged to `release`. Say the word for a PR/merge.
- **New migrations** applied on startup: `UserCalendarRowOrder` (reorder), `AddEmailConfigOverrideEnabled` (4.3), `AddUserMonitoredShift` (4.5).
- **Test-data residue in `app.db`** (regenerable QA data, delete `app.db` to re-seed): force-delete verification removed throwaway QA users #83 & #137 (their chores/on-duties reassigned to `admin@local`); various verification runs left assignments/notes/reorder rows and a monitored-shift selection for `admin@local`.
- **Dev gotchas re-confirmed this session** (in project memory): the app serves JS/CSS via **WebOptimizer** (minify + per-process cache) — restart to pick up edits, verify served assets by a **string literal** not a function name; the **`-- xUnit.ParallelizeTestCollections=false`** flag is flaky (one run showed 231 spurious failures, a clean re-run gave full green); **always grep a resx key in both files before adding** (a duplicate `Next7Days` broke 6 localization tests).

## Specs/plans produced this session (for reference)
- `docs/superpowers/specs/2026-06-12-per-user-calendar-row-reorder-design.md` + `docs/superpowers/plans/2026-06-12-per-user-calendar-reorder.md` (reorder feature)
- `docs/superpowers/specs/2026-06-13-phase4-design-proposals.md` (Phase 4 — superseded; all built)

## Recommended next-session priorities
1. Review + merge `dev` → `release` (or PR) once you've eyeballed the work.
2. Quick wins from deferred list: 4.1 approver dropdown (#1), live Test-Summon check (#2).
3. Decide the 1.2 grant-less-notes policy (#4) and confirm the 4.3 disable semantics.
4. Optionally regenerate `app.db` to clear test residue.
