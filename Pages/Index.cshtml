@page
@model ShiftManager.Pages.IndexModel
@* "/" redirects to /Home/Index (see IndexModel.OnGet) — Home is the schedule-spine landing for
   everyone under the domain-hub nav. This view is intentionally empty; it never renders. *@
