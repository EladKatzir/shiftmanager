@page
@model ShiftManager.Pages.Calendar.ManageDistributionListsModel
@{
    // AJAX-only endpoint for the distribution-list manager modal (see Shifts calendar).
    // All meaningful responses come from the named handlers (?handler=MoleculeUsers / List / Create / Update / Delete).
    Layout = null;
}
